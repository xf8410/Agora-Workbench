name: Legacy GitHub confirmation and memory-sync patcher (disabled)

# Historical source generator retained only for audit. It must not synthesize files or
# push directly to main; future memory-sync work belongs on a reviewed workbench branch.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Disabled: source generation and direct main pushes are prohibited."
          exit 1
