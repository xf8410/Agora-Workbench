name: Legacy GitHub create-repository patcher (disabled)

# This workflow checked out a fixed feature branch and applied textual source patches.
# It is retained as history only and cannot mutate any branch.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Disabled: implement and review the tool in normal source commits."
          exit 1
