name: Validate public repository browser

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Maintenance notice
        run: echo "This legacy branch validation workflow has been retired."
