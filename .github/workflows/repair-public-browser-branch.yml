name: Repair public browser branch

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Maintenance notice
        run: echo "This one-time repair workflow has been retired."
