name: Apply separate public repository read

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Maintenance notice
        run: echo "This one-time public-read patch workflow has been retired."
