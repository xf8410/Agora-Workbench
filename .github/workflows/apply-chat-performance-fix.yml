name: Legacy chat performance source patch (disabled)

# Disabled: source-generating workflows must not mutate main or overwrite newer code.
# The historical script remains in git for audit only; apply changes through a reviewed
# workbench/* branch and the normal build/integration path.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Disabled: port the required change to a workbench/* branch and validate it there."
          exit 1
