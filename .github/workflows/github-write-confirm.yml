name: Legacy GitHub confirmation patcher (disabled)

# This patch workflow is obsolete and could apply stale textual replacements. Keep the
# runtime confirmation implementation in reviewed source instead.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Disabled: stale source patch workflows cannot modify main."
          exit 1
