name: Deprecated - client resilience integration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Disabled: client resilience source is already integrated. This workflow cannot modify repository content.'
