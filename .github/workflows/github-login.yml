name: Legacy GitHub login source generator (disabled)

# Kept only as a historical marker. Runtime source must be edited and reviewed normally;
# this workflow may not rewrite Kotlin/UI files or push main.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Disabled: source generation and direct main pushes are prohibited."
          exit 1
