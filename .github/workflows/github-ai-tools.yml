name: Legacy GitHub AI source generator (disabled)

# Historical versions of this workflow generated complete Kotlin files and pushed them
# directly to main. That can overwrite newer implementations and is intentionally disabled.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Disabled: implement GitHub tools in source on a workbench/* branch."
          exit 1
